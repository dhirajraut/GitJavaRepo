package com.intertek.service;

import com.intertek.entity.GLAccount;
import com.intertek.entity.GLDepartment;
import com.intertek.entity.GLProductGroup;

public class GLServiceImpl extends BaseService implements GLService
{
  public void saveGLAccount(GLAccount glAccount)
  {
    getDao().save(glAccount);
  }

  public void saveGLDepartment(GLDepartment glDepartment)
  {
    getDao().save(glDepartment);
  }

  public void saveGLProductGroup(GLProductGroup glProductGroup)
  {
    getDao().save(glProductGroup);
  }
}

